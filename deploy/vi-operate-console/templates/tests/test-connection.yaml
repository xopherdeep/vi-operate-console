apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vi-operate-console.fullname" . }}-test-connection"
  labels:
    {{- include "vi-operate-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vi-operate-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
