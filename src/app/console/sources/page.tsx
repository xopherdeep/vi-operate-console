import { Metadata } from 'next';
import SourcesClientPage from './page.client';

export const metadata: Metadata = {
  title: 'Data Sources',
  description: 'Add, manage and monitor your data source connections'
};

export default SourcesClientPage;
