'use client'

import { ActionButtonProps } from "@/components/common/layout"

export const actionButton = {
  label: 'Create Report',
  onClick: () => console.log('Create report clicked')
} as ActionButtonProps 